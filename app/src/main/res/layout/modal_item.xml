<!-- res/layout/dialog_item.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dialogContainer"
    android:orientation="horizontal"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/rounded_button"> <!-- fond par défaut, modifiable -->

    <ImageView
        android:id="@+id/itemImage"
        android:layout_width="106dp"
        android:layout_height="103dp"
        android:background="@drawable/round"
        android:clipToOutline="true"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_erable" />

    <LinearLayout
        android:orientation="vertical"
        android:layout_marginStart="16dp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1">

        <TextView
            android:id="@+id/itemNom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Nom"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/itemType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Type"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/itemEffet"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Effets"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/expiration"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Expiration"
            android:textSize="10sp" />

        <Button
            android:id="@+id/btnFermer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/btn_fermer" />
    </LinearLayout>
</LinearLayout>
